import 'package:flutter/material.dart';

class AppColors {
  // Brand colors
  static const Color primary = Color(0xFF0066CC);
  static const Color secondary = Color(0xFF009966);
  
  // Status colors
  static const Color positive = Color(0xFFD32F2F);
  static const Color negative = Color(0xFF388E3C);
  static const Color invalid = Color(0xFF757575);
  
  // Neutral colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;
  static const Color error = Color(0xFFB00020);
}
