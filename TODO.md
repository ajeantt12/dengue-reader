# to-do list

1. add caliberation fixes to the latest apk build
2. add a flag button to flag incorrectly calculated results (this should be available on the app, to be flagged by the researchers and exported in the metadata file)
3. add an export data button for the app to export all it's current results in a .csv format that can be shared back to me for further tesing and caliberation. 
4. add markers for well circles on the plate (3x3 grid, based on DR008.jpeg) and also possibly a marker or overlay to align the wells with the colour coding strips (also visible in DR008.jpeg)
