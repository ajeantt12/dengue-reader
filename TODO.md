# to-do list

1. ~~Change the diagram on the test results to denote the new approach.~~ Done —
   `ResultCalculator` now derives the reactive threshold from Row 1 (positive
   control) and Row 2 (negative control), and judges Row 3 (the sample) against
   it. `DotGridDisplay` labels the rows accordingly and colours wells from that
   calibrated threshold instead of a fixed cutoff.
2. Add image upload option along with click as well.
3. Is it possible for us to have various orientations of the colour reference plate, and the wells. For, different plates that we have, or should we just stick to our decided overlay
4. The export data should download image files as well. Make sure all images shot are stored within the app's data too.
5. Make sure, when I send an .apk file to the research team to update the app, the data isn't lost – or tell me if I should ask them to export data everytime before updating.
6. Add a button to toggle the timer on and off, the timer that shows up before taking the photo.
